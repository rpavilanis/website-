
<% @posts.each do |post| %>
  <div class="blog-format">
    <p><%= post.created_at.strftime("%A, %b, %d") %></p>
    <p><%= link_to post.title, post %></p>
  </div>
<% end %>

<%= will_paginate @posts %>
